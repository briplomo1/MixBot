name: bluetooth_example
description: A new Flutter project.

publish_to: 'none' 
version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  json_annotation: ^3.0.1
  flutter:
    sdk: flutter

  cupertino_icons: ^0.1.3

  flutter_bluetooth_serial: ^0.2.2

  

dev_dependencies:
  build_runner: ^1.0.0
  json_serializable: ^3.3.0
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
